name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Déclencher le déploiement
        run: |
          curl -X POST https://benraffstudio.com/deploy \
            -H "x-deploy-token: benraff_deploy_2026"
