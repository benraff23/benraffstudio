name: Deploy to VPS

# Déploiement désactivé — déclenché manuellement via deploy.sh depuis le PC local
# (Hostinger bloque les IPs GitHub/Azure)
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Lancer deploy.sh depuis le PC local pour déployer."
